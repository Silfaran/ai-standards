# task-service (fixture placeholder)

Empty stub directory. The dynamic smoke harness turns this into a git repo
on `master` at runtime so the orchestrator's pre-flight branch check passes.

No real code — this fixture never reaches the Backend Developer phase (the
capture hook denies the first `Agent` spawn after recording its arguments).
